
  # Convert to code

  This is a code bundle for Convert to code. The original project is available at https://www.figma.com/design/UwIepFxwPA6lTQofoWmcfh/Convert-to-code.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  